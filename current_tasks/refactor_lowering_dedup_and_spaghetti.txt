Task: Refactor IR lowering code - deduplication and spaghetti cleanup

Focus areas:
1. Add CType helper methods to eliminate repeated pattern matches across lowering files
2. Unify duplicated extract_func_ptr_return_ctype functions (expr.rs + expr_types.rs)
3. Add FuncSig::basic() constructor to eliminate 10-field boilerplate construction
4. Deduplicate global_ty determination logic (lowering.rs + stmt.rs)
5. Extract transparent_union marking logic (duplicated in lowering.rs)
6. Add ctype_size convenience method to reduce size_ctx boilerplate
7. Deduplicate global emit helper (IrGlobal construction pattern)
8. General cleanup of duplicated code patterns across lowering files

NOT doing: emit_struct_init decomposition (too risky, separate task)
