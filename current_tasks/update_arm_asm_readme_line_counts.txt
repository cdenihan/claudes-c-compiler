Task: Update stale line counts in ARM assembler README.md

The line counts in the file inventory and ASCII diagram are stale.
Updating parser.rs ~2,340 -> ~2,600, encoder.rs ~6,050 -> ~6,150,
elf_writer.rs ~570 -> ~580, total ~9,200 -> ~9,600.
