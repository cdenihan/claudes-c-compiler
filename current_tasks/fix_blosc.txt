Task: Fix blosc project compilation and tests
Status: in_progress

Issues:
1. -mavx2 flag doesn't define __AVX2__ macro, causing blosc AVX2 shuffle
   code to compile as abort() stubs instead of real implementations
2. Missing several AVX2 intrinsics needed by blosc's shuffle-avx2.c

Fix plan:
- Add CLI flag handling for -mavx2, -mavx, -msse3, -msse4.1, -msse4.2
  to define corresponding __AVX2__, __AVX__, etc. macros
- Add missing AVX2 intrinsics to avx2intrin.h
